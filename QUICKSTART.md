# 🚀 Быстрый старт

## Первый запуск

### 1. Установка зависимостей

```bash
# Python зависимости
pip install -r requirements.txt

# Node.js зависимости (для Tailwind CSS)
npm install

# Сборка CSS
npm run build:css
```

### 2. Настройка переменных окружения

Создайте файл `.env` из примера:

```bash
cp .env.example .env
```

Заполните в `.env`:
- `FLASK_SECRET_KEY` - секретный ключ Flask
- `BITRIX24_CLIENT_ID` и `BITRIX24_CLIENT_SECRET`
- `YANDEX_CLIENT_ID` и `YANDEX_CLIENT_SECRET`
- `YANDEX_OAUTH_TOKEN` - постоянный токен для Яндекс Телемост

### 3. Запуск приложения

```bash
python run.py
```

Приложение будет доступно по адресу: http://localhost:5000

## Разработка

Для разработки используйте watch-режим для автоматической пересборки CSS:

```bash
# Терминал 1 - watch CSS
npm run watch:css

# Терминал 2 - Flask dev server
python run.py
```

## Production деплой

1. Соберите CSS:
```bash
npm run build:css
```

2. Запустите через WSGI сервер (gunicorn):
```bash
gunicorn -w 4 -b 0.0.0.0:5000 run:app
```

## Структура проекта

```
telemost-bitrix-app/
├── app.py                 # Flask приложение
├── config.py              # Конфигурация
├── database.py            # Работа с БД
├── models/                # API модели
│   ├── bitrix24.py        # Bitrix24 API
│   └── telemost.py        # Yandex Telemost API
├── templates/             # HTML шаблоны
│   └── index.html         # Основная страница
├── static/                # Статические файлы
│   └── css/
│       ├── input.css      # Входной Tailwind CSS
│       └── output.css     # Скомпилированный CSS (18KB)
├── package.json           # npm зависимости
├── tailwind.config.js     # Конфигурация Tailwind
└── requirements.txt       # Python зависимости
```

## Основные функции

### Создание конференции/трансляции
1. Нажмите "Создать"
2. Выберите тип: Конференция или Трансляция
3. Заполните название (обязательно)
4. Добавьте соорганизаторов (опционально)
5. Нажмите "Создать"

### Управление событиями
- **Начать** - открывает конференцию в новой вкладке
- **Ссылка** - копирует ссылку в буфер обмена
- **⋯** → **Редактировать** - изменить событие
- **⋯** → **Удалить** - удалить событие

### Фильтрация
- **Все** - показывает все события пользователя
- **Конференции** - только конференции
- **Трансляции** - только трансляции
- **Поиск** - поиск по названию

## Особенности

✅ **Персональные события** - каждый пользователь видит только свои конференции
✅ **Быстрая загрузка** - CSS оптимизирован (18KB вместо 3MB)
✅ **Простые формы** - только название и соорганизаторы
✅ **Toast уведомления** - успех, ошибки, предупреждения
✅ **Интеграция с Bitrix24** - автоматическая аутентификация

## Поддержка

Для вопросов и проблем создайте issue в репозитории.

## Changelog

См. [CHANGELOG.md](CHANGELOG.md) для истории изменений.

## Деплой

См. [DEPLOYMENT.md](DEPLOYMENT.md) для инструкций по развертыванию.

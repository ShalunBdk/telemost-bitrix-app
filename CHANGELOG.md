# Changelog

## [1.1.0] - 2025-12-09

### ✨ Улучшения

#### Оптимизация производительности
- **Миграция с CDN Tailwind на production сборку**
  - Размер CSS уменьшен с ~3000KB до 18KB (~167x быстрее)
  - Удалено предупреждение "should not be used in production"
  - Добавлена автоматическая сборка через Tailwind CLI

#### Исправления пользовательского опыта
- **Правильная идентификация владельцев событий**
  - Добавлена функция `get_current_bitrix_user()` для получения ID пользователя
  - Owner_id и owner_name теперь сохраняются корректно из Bitrix24 API
  - Исправлена проблема с сохранением "unknown" и "Unknown User"

- **Персональные события**
  - Добавлена фильтрация событий по текущему пользователю
  - Каждый пользователь видит только свои конференции и трансляции
  - Использована функция `get_user_conferences(user_id)` вместо `get_all_conferences()`

- **Упрощение форм создания**
  - Удалены избыточные поля: описание, дата, время, настройки трансляции
  - Оставлены только: название (обязательное) и соорганизаторы (опционально)
  - API отправляет минимальный набор данных: type, name, cohosts

- **Обновление интерфейса**
  - Удалены упоминания "события общие для всех"
  - Обновлены описания типов событий
  - Улучшена ясность интерфейса

### 🐛 Исправления багов

- **Исправлена ошибка `ReferenceError: showToast is not defined`**
  - Добавлена функция `showToast()` в Vue компонент
  - Реализована система toast-уведомлений с автоудалением
  - Поддержка типов: success, error, warning, info

### 📦 Новые файлы

- `package.json` - npm зависимости для Tailwind CLI
- `tailwind.config.js` - конфигурация Tailwind CSS
- `static/css/input.css` - входной файл для Tailwind
- `static/css/output.css` - скомпилированный CSS (18KB)
- `.gitignore` - игнорирование node_modules и временных файлов
- `DEPLOYMENT.md` - инструкции по развертыванию
- `CHANGELOG.md` - история изменений

### 📝 Обновленные файлы

- `app.py` - добавлены функции для работы с пользователями Bitrix24
- `templates/index.html` - упрощены формы, добавлен showToast, подключен локальный CSS
- `README.md` - обновлены инструкции по установке и сборке

### 🔧 Технические детали

#### Backend (app.py)
- Добавлена функция `get_current_bitrix_user()` (строки 75-116)
- Обновлена `get_conferences()` с фильтрацией по user_id (строки 342-441)
- Упрощена `create_conference()` для отправки минимальных данных (строки 443-531)

#### Frontend (index.html)
- Упрощена модель `defaultNew()` - только type, name, cohosts (строки 351-355)
- Добавлена функция `showToast()` с автоудалением (строки 552-579)
- Удалены неиспользуемые поля формы (строки 281-322)

#### Стили (static/css/)
- Перенесены кастомные стили из `<style>` в `input.css`
- Скомпилирован минифицированный `output.css` (18KB)

### 🚀 Команды

```bash
# Установка зависимостей
npm install

# Production сборка CSS
npm run build:css

# Development режим (watch)
npm run watch:css
```

### ⚡ Производительность

- Загрузка CSS: **~167x быстрее**
- Размер CSS файла: **3000KB → 18KB**
- Время первой загрузки страницы: значительно сокращено

---

## [1.0.0] - Initial Release

- Базовая интеграция с Bitrix24 и Yandex Telemost
- Создание видеоконференций и трансляций
- Управление участниками

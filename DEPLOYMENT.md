# Инструкция по развертыванию

## Подготовка к деплою

### 1. Установка зависимостей

```bash
# Python зависимости
pip install -r requirements.txt

# Node.js зависимости (для Tailwind CSS)
npm install
```

### 2. Сборка статических файлов

```bash
# Production сборка CSS (минифицированный)
npm run build:css
```

**Важно:** Скомпилированный файл `static/css/output.css` должен быть загружен на сервер!

### 3. Переменные окружения

Убедитесь, что файл `.env` содержит все необходимые переменные:

```env
FLASK_SECRET_KEY=ваш-секретный-ключ
BITRIX24_CLIENT_ID=...
BITRIX24_CLIENT_SECRET=...
YANDEX_CLIENT_ID=...
YANDEX_CLIENT_SECRET=...
YANDEX_OAUTH_TOKEN=...
```

### 4. Запуск в продакшене

Используйте WSGI сервер (например, gunicorn):

```bash
gunicorn -w 4 -b 0.0.0.0:5000 run:app
```

## Разработка

Во время разработки используйте автоматическую пересборку CSS:

```bash
# В одном терминале - watch режим для CSS
npm run watch:css

# В другом терминале - Flask dev server
python run.py
```

## Размер файлов

- **CDN Tailwind**: ~3MB (не кэшируется между сайтами)
- **Скомпилированный CSS**: ~18KB (минифицированный, содержит только используемые классы)

**Улучшение скорости загрузки**: ~165x быстрее! 🚀

## Обновление стилей

После изменения HTML или добавления новых Tailwind классов:

```bash
npm run build:css
```

Это пересоберет CSS с учетом всех используемых классов.
